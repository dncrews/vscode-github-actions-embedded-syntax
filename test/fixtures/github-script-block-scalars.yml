name: test

on: workflow_dispatch

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v8
        with:
          script: |-
            const x = 1;
      - uses: actions/github-script@v8
        with:
          script: |+
            const y = 2;
      - uses: actions/github-script@v8
        with:
          script: |2
            const z = 3;
      - uses: actions/github-script@v8
        with:
          script: >-
            const a = 4;
      - uses: actions/github-script@v8
        with:
          script: >+
            const b = 5;
      - uses: actions/github-script@v8
        with:
          script: |
            const before = true;

            const after = true;
